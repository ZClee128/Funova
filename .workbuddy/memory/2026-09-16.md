# 2026-09-16 工作日志

## iOS 提审流水线（Funova / com.mmhua.com）
- 目标：把 iOS 包推到 GitHub 并用 GitHub Actions 提审 App Store。
- 已建本地 git 仓库（main，commit ad04ea3），并推送到 https://github.com/ZClee128/Funova.git 。
- 工程组成：`.github/workflows/ios-submit.yml`（macOS runner + fastlane，tag v* 或手动触发）、`fastlane/Fastfile`（`upload_ipa` 传现成 IPA / `build_and_upload` 从源码打包）、`fastlane/Appfile`、`Gemfile`、`.gitignore`、`README.md`。
- 核查结论：`.ipa` 已用 App Store 分发证书签名（团队 Pham Thi Nhung / S88VU4PBFB，bundle id com.mmhua.com，有效期 2027-08-31），可直接上传无需重签；工作区无 Xcode 源码，只能走"传现成 IPA"；`开发证书.p12` 为开发证书且含私钥，已 gitignore 不提交。
- 待办（用户侧）：① 仓库 Secrets 加 App Store Connect API Key（KEY_ID / ISSUER_ID / P8）；② App Store Connect 后台填齐该版本元数据；③ 打 tag 或 Actions 页 Run workflow 触发提审。

## 备注
- 本机 git credential.helper=store 存有可用 GitHub 凭据，HTTPS push 可直接成功；gh CLI 未登录但不影响。
